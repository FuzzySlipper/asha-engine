# Files/dirs allowed to keep legacy WorldState/WorldBundle-family naming.
# These predate the ECRP vocabulary ADR (Den: asha/ecrp-vocabulary-taxonomy).
# Target vocabulary: SessionState/RuntimeSession for live authority,
# ProjectBundle for durable saved content.
#
# Entries ending in '/' cover the whole directory. Do not add new entries for
# new code — new code uses the current vocabulary, or an inline
# 'vocab-allow: <reason>' marker when it must name a legacy type deliberately
# (e.g. migration shims). Remove entries as files migrate.

# Legacy crates (whole-crate: world-bundle is their current domain language)
engine-rs/crates/rules/rule-world-bundle/
engine-rs/crates/services/svc-serialization/
engine-rs/crates/tools/scene-diagnostics/

# Legacy touchpoints in otherwise-current crates
engine-rs/crates/state/core-catalog/src/lib.rs
engine-rs/crates/state/core-entity/src/persist.rs
